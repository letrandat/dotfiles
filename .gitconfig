[user]
	name = Tran Dat, Le
	email = trandatnh@gmail.com
[core]
	autocrlf = input
	# use vscode as editor
	editor = code --wait

# use vscode as diff and merge tool
[diff]
    tool = default-difftool
[difftool "default-difftool"]
    cmd = code --wait --diff $LOCAL $REMOTE
[merge]
    tool = code
[mergetool "code"]
    cmd = code --wait --merge $REMOTE $LOCAL $BASE $MERGED
